# Docsy 1.0.5 发布检查记录

日期：2026-09-16。本记录对应 1.0.5 发布源码；以下区分自动化检查、真实文件回归和仍需人工覆盖的场景。

## 自动化验证

- 前端：34 个测试文件，253 项通过。
- Rust library：346 项通过，11 项默认忽略；忽略测试未计入通过数。
- 官网数据生成：Python 回归测试通过，覆盖发布过滤、架构分类、校验值与远程下载链接；应用五处版本号及发布标签自动一致性检查通过。
- 全项目 ESLint：无错误、无警告；Vite 生产构建和 macOS DMG 打包成功，存在既有包体积提示。Windows 安装包由发布工作流构建并核对，不将本地 macOS 检查等同于 Windows 运行验收。
- 两份真实证据 PDF：本轮各运行三个显式回归（共六项通过），验证几何纠偏、仅清除所选页脚且原件字节不变、页段建议/拆分/分组重合并及分项纯合并，分别恢复 100 / 159 页。未做逐页视觉对比。
- 检查涉及证据、普通 PDF、Markdown 转换、视频抽帧、图片排版、模板、共享拖拽和子进程管理；检查深度不同，不等同于所有界面端到端验收。

## 本轮新增修复

本轮在前述修复上，追加检查了六个业务模块、首页、设置和组件安装界面。修复 PDF 防复制检测字段错配、错误恢复源文件、压缩 CMap 读取和同名覆盖；模板预览状态/刷新绑定、批量生成模板与历史隔离；批量任务取消后继续执行；图片旧结果回填；视频结果目录切换；安装复检失败误报成功。对应回归纳入上述统计。

1. 窗口级拖拽监听忽略不可见组件，防止隐藏证据标签页同时导入文件。
2. 子进程注册器捕获 stdout/stderr，避免应用运行时 qpdf 页数输出丢失；注册键包含子进程 PID，避免并发同类操作覆盖登记。新增真实 shell 输出捕获测试。
3. 普通 PDF 手动拆分页数读取失败不再使用虚假的 1 页；页数请求返回时检查当前文件；禁止重复拆分。
4. 视频抽帧期间禁止切换/清空视频及重复启动；旧视频探测结果不再回填不同文件。
5. Markdown 队列收到取消结果后停止追加任务，不再将取消提示为成功。

## 已有修复与限制

- 本轮界面验证使用真实 Vue 组件和模拟检测返回值，在 1360×820、1100×680 浏览器视口检查按钮、统计卡、内联页段表及共用标记弹窗；不等同于桌面端真实文件拆分验收。
- 实际点击验证六项元素全选并标记删除、计数回写、元素预览跳至第 50 页和重复渲染；用同名不同路径 PDF 验证同序列仅选本文件、筛选切换清空旧选择。
- 分项证据的批量元素检测测试覆盖六份同名文件、最多四份并发、乱序返回与单份失败。
- 合并证据拆分新增顺序队列，复用单文件后端；新增测试覆盖追加导入、文件设置与撤销隔离、顺序检测、仅拆分已核对文件、独立输出目录、部分失败只重试剩余页段、停止队列、读取页数失败重试及移出分组。
- 浏览器使用两份同名 PDF 的模拟响应，完成多文件导入、顺序检测、按文件选择六项清除标记、核对后顺序拆分；两组输出各三份文件，分别展示来源路径、输出目录及页段。尚未在桌面端运行本轮多文件队列的真实文件验收。
- 透明小狗与提示气泡已分离；复用同一份约 841 KiB 图集接入八种动态，补充短摇摆和小跳效果，本轮未生成新画稿或增加 GIF/视频资源。首页、非紧凑空态、任务气泡、组件配置、待检测及拆分完成区已接入；紧凑文件队列保留原图标。
- 动作预览使用真实 Vue 组件；浏览器验证八种动作播放、首页点击气泡、完成/错误姿态切换和离屏暂停，控制台无错误。支持减少动态效果的 CSS 已加入，但本轮未切换系统偏好验证；未做桌面端性能基准测试。
- 界面测试发现并修复 PDF.js worker URL 动态导入失效及 v6 文档销毁接口变更；浏览器内首次预览、跳页与重复渲染已验证。

- 证据三模块修复详情见 CHANGELOG.md；大文件内容检测仍由用户手动触发。
- Node 不在终端 PATH 是独立环境问题；本轮命令临时使用已存在的 Node 运行时，未修改 shell 配置。
- Rust 存在旧 Artifact 编辑辅助函数未使用警告；没有用允许警告属性掩盖。
- 单纯页数与结构回归无法证明签章外观、旋转、字体和每一页内容都正确。
- 没有做依赖漏洞数据库在线扫描，不能声称第三方依赖无漏洞。

## 停更前尚需验收

- 从当前源码重新打包，在真实桌面应用内逐模块完成导入、执行、输出打开、取消、失败重试。
- macOS 与 Windows 分别验证；本机结果不能代表 Windows x64/x86。
- Word/WPS/LibreOffice 实际转换、PDF 缺字体与扫描件、超大文件、低磁盘空间和损坏文件。
- 图片排版 PDF/DOCX 和模板批量导出做视觉复核；视频实际抽帧与取消复核。
- 确认全部输出不覆盖原件，检查发布版本、安装包及下载站一致后再发布。

结论：自动化基线通过，但尚不满足“全功能已验收、可无条件停更”的声明。
