# Docsy 全模块审核报告

审核日期：2026-07-30

## 总览

| 模块 | 状态 | 关键问题 |
|------|------|---------|
| **模板引擎** (template) | ✅ 完整 | select 类型已补齐；批量填写已实现 |
| **PDF 工具** (pdf-tools + evidence-pdf) | ⚠ 功能完整，有质量缺口 | 前端 73KB 单体组件；纯 Rust 迁移未启动 |
| **图片处理** (image-paddler) | ⚠ 功能完整，有 2 个 bug | 前端 args 嵌套问题；filename_remove_text 被覆盖 |
| **视频提取** (video-extract) | ✅ 完整 | 仅小问题：ffprobe 超时、死代码 |
| **设置** (settings) | ⚠ 核心功能完整 | 模块注册设置是死代码；外部工具有平台 bug |
| **首页** (home) | ✅ 完整 | 无关键问题 |
| **文档/架构** | ⚠ 需更新 | README 版本号/测试数过时；CHANGELOG 停在 v0.5.3 |

---

## 一、模板引擎

### 已实现（10/12 设计点完整）

- ✅ 7 种字段类型（text/date/select/party_list/checkbox/radio_group/checkbox_group）
- ✅ party_list 表格行复制
- ✅ 空值规则（前缀/后缀删除 + 作用域控制）
- ✅ 扫描页眉页脚
- ✅ 稳定定位（w:sdt 内容控件）
- ✅ 勾选框（marker/label 分离 + 字体保留）
- ✅ 历史与建议（4 种建议源）
- ✅ 标记自动合并
- ✅ 字段推断
- ✅ 双栏预览
- ✅ 日期格式（6 种）
- ✅ 批量填写（本轮新增）

### 未实现

- ⚠ 多模板材料包（设计文档列为后续优先扩展）

### 本轮修复

1. reference 字段前缀替换问题
2. 案号前缀/后缀 fallback
3. 预览全文段落化
4. 回收站模板历史过滤
5. select 类型字段支持
6. 批量填写功能（导出/校验/导入生成）

---

## 二、PDF 工具

### 功能清单

| 功能 | 状态 |
|------|------|
| 证据 PDF 导入 | ✅ |
| 页眉页脚检测（3 层） | ⚠ 2/3 层（缺注解层） |
| 页眉页脚插入/编辑 | ✅ |
| CJK 字体嵌入 | ✅ |
| 连续页码 | ✅ |
| A4 规范化 | ⚠ 仅矩阵缩放，无"保留文本优先"模式 |
| 批注删除 | ✅ |
| 合并输出 | ✅ |
| 拆分（按页眉变化 + 手动范围） | ✅ |
| 预览（pdftoppm + PDF.js） | ✅ |
| 非破坏性输出 | ✅ |

### 关键问题

1. **前端单体组件**：`EvidencePdfWorkbench.vue` 73KB，设计文档要求拆分 6 个子组件
2. **纯 Rust 迁移未启动**：设计文档规划 6 个阶段，lopdf/hayo/pdf-syntax 均未替换 qpdf CLI
3. **XObject 内部 artifact 扫描**未实现
4. **页码自动修正**未实现
5. **长任务无进度/取消**：所有命令是同步阻塞

### 代码质量问题

- detection.rs 临时文件未用 TempPathGuard
- page_info.rs 的 qpdf JSON 解析较脆弱
- content_text.rs 不跟踪 CTM

---

## 三、图片处理 (image-paddler)

### 功能完整度

| 功能 | 状态 |
|------|------|
| 文件夹扫描（7 种图片格式） | ✅ |
| 多文件夹支持 | ✅ |
| 分析 + 推荐布局 | ✅ |
| PDF 生成（含 CJK） | ✅ |
| DOCX 生成 | ✅ |
| 文件名规则引擎（5 种规则） | ✅ |
| 布局 + 排序模式 | ✅ |
| 边框支持 | ✅ |
| 实时预览 | ✅ |
| 拖放支持 | ✅ |
| 单元测试（12 个） | ✅ |

### Bug

1. **前端 run() 参数嵌套问题**：`tauriCallSafe` 的 args 可能多套一层
2. **filename_remove_text 被覆盖为空字符串**：用户设置被忽略

### 质量问题

- 前后端重复实现 parseLayout/reorderImages 等函数
- sanitize_output_name 的 CJK 范围不完整
- 字符串枚举无验证

---

## 四、视频提取 (video-extract)

### 功能完整度

| 功能 | 状态 |
|------|------|
| FFmpeg 检测 + 安装 | ✅ |
| 视频探测（时长/分辨率/帧率/编码） | ✅ |
| 帧提取（fps/间隔两种模式） | ✅ |
| 时间范围选择 | ✅ |
| 格式/质量控制 | ✅ |
| 时间戳水印 | ✅ |
| 结果预览网格 | ✅ |
| 拖放加载 | ✅ |
| 单元测试（5 个） | ✅ |

### 小问题

- ffprobe 的 `-v quiet` 导致空闲超时等同于总超时
- `probe_video` 返回未使用的 raw ffprobe JSON
- `list_system_fonts` 是死代码
- 水印字号硬编码 24

---

## 五、设置 + 首页

### 设置模块

| 功能 | 状态 |
|------|------|
| 应用设置 CRUD | ⚠ 仅 2 个字段，无脏状态追踪 |
| 模块注册设置 | ⚠ 框架已搭但从未使用（死代码） |
| 外部工具检测（qpdf/ffmpeg/poppler） | ✅ |
| 外部工具检测（LibreOffice） | ⚠ 缺 .exe 后缀（Windows） |
| 外部工具检测（Word/WPS） | ⚠ reg query 无超时 |
| 外部工具自动安装 | ✅ 安全性好（SHA256、zip-slip 防护） |

### 首页模块

- ✅ 英雄横幅 + 动态卡片网格
- ✅ 设置驱动的可见性/排序

### 外部工具 Bug

1. WPS Writer 返回 COM ProgID 作为 PathBuf
2. Word/WPS 的 `reg query` 无超时
3. macOS/Linux 无嵌入式离线安装包

---

## 六、文档一致性

### README.md 过时项

| 项目 | README 写的 | 实际 |
|------|------------|------|
| 版本号 | 0.8.1 | **0.8.2** |
| Rust 测试数 | 154 | **121** |
| 模板引擎文件 | 9 个（缺 batch.rs/mod.rs） | **11 个** |
| 批量填写功能 | 未提及 | **已实现** |

### architecture.md 过时项

| 项目 | 文档写的 | 实际 |
|------|---------|------|
| 前端模块列表 | 6 个 | **7 个**（缺 evidence-pdf） |
| 后端命令域 | 5 个 | **6 个**（缺 template） |
| 批量填写 | 未提及 | **已实现** |

### CHANGELOG

- 停在 v0.5.3（2026-06），v0.6~v0.8 无记录

### 设计文档 vs 实现

| 设计文档 | 实现状态 |
|---------|---------|
| 模板系统设计 | ✅ 95% 实现 |
| PDF 证据处理设计 | ⚠ 80% 实现 |
| 纯 Rust 化方案 | ✗ 未启动 |
| 案件仪表盘设计 | ✗ 未启动 |
| UI 重建计划 | ⚠ 2/6 组件 |
| 飞书数据分析 | ✗ 仅数据，无工具 |

---

## 优先修复建议

### P0（影响功能）

1. image-paddler 前端 args 嵌套 bug
2. image-paddler filename_remove_text 被覆盖
3. WPS Writer PathBuf bug
4. Word/WPS reg query 超时

### P1（影响质量）

5. EvidencePdfWorkbench.vue 组件拆分
6. PDF 长任务进度/取消
7. 模块注册设置死代码清理
8. README/architecture 文档更新

### P2（长期）

9. 纯 Rust PDF 迁移
10. 案件仪表盘实现
11. UI 重建剩余 4 个组件
12. 多模板材料包
